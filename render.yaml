services:
  - type: web
    name: briscola-ai
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.14.3"
      - key: BRISCOLA_EVENT_LOG_MODE
        value: "off"
